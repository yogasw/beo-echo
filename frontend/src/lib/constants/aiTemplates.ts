/**
 * Default prompt template for generating mock response data
 */
export const DEFAULT_GENERATE_PROMPT = `Generate realistic mock JSON response data for an API endpoint. The response should be valid JSON with varied and realistic data. Include appropriate fields like id, name, timestamps, etc. Make it look like real API data.`;
